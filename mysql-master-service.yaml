apiVersion: v1
kind: Service
metadata:
  name: mysql-master
  namespace: mysql
  labels:
    app: mysql-master
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql-master
  clusterIP: None  # Headless Service，用于 StatefulSet
